---
import Layout from '@/layouts/Layout.astro';
import Podcast from '@/components/Podcast';

const { id } = Astro.params;
---

<!-- TODO: fetch this in component and update page title --><!-- <Layout title={`${podcast.title} by ${podcast.author} - Astro Podcasts`}> -->
<Layout title={'testing'}>
  <Podcast client:only id={id} />
</Layout>
